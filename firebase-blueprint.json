{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "Basic user account information.",
      "type": "object",
      "properties": {
        "email": { "type": "string" },
        "businessId": { "type": "string" },
        "role": { "type": "string", "enum": ["owner", "manager", "sales_person", "inventory", "accountant"] },
        "branchId": { "type": "string" },
        "name": { "type": "string" },
        "phone": { "type": "string" },
        "status": { "type": "string", "enum": ["active", "suspended"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["email", "name", "businessId"]
    },
    "Business": {
      "title": "Business Registration",
      "description": "Owner's business details.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "category": { "type": "string" },
        "county": { "type": "string" },
        "town": { "type": "string" },
        "phone": { "type": "string" },
        "logoUrl": { "type": "string" },
        "ownerEmail": { "type": "string" },
        "ownerUid": { "type": "string" },
        "settings": { "type": "object" },
        "status": { "type": "string", "enum": ["active", "suspended"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "category", "ownerEmail"]
    },
    "Branch": {
      "title": "Business Branch",
      "description": "Physical branch location.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "location": { "type": "string" },
        "phone": { "type": "string" },
        "managerId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name"]
    },
    "Product": {
      "title": "Product",
      "description": "Inventory item.",
      "type": "object",
      "properties": {
        "businessId": { "type": "string" },
        "branchId": { "type": "string" },
        "name": { "type": "string" },
        "sku": { "type": "string" },
        "barcode": { "type": "string" },
        "categoryId": { "type": "string" },
        "unit": { "type": "string" },
        "buyingPrice": { "type": "number" },
        "sellingPrice": { "type": "number" },
        "imageUrl": { "type": "string" },
        "vatApplicable": { "type": "boolean" },
        "minStock": { "type": "number" },
        "stock": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "sellingPrice", "businessId", "branchId"]
    },
    "Transaction": {
      "title": "Sale Transaction",
      "description": "Record of a sale.",
      "type": "object",
      "properties": {
        "businessId": { "type": "string" },
        "branchId": { "type": "string" },
        "cashierId": { "type": "string" },
        "customerId": { "type": "string" },
        "items": { "type": "array" },
        "subtotal": { "type": "number" },
        "discount": { "type": "number" },
        "tax": { "type": "number" },
        "total": { "type": "number" },
        "amountPaid": { "type": "number" },
        "balanceRemaining": { "type": "number" },
        "status": { "type": "string", "enum": ["paid", "partial", "credit"] },
        "notes": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["businessId", "branchId", "cashierId", "total"]
    }
  },
  "firestore": {
    "/users/{uid}": { "schema": "User", "description": "Global user profiles." },
    "/businesses/{businessId}": { "schema": "Business", "description": "Business settings and status." },
    "/businesses/{businessId}/branches/{branchId}": { "schema": "Branch", "description": "Branches under a business." },
    "/businesses/{businessId}/branches/{branchId}/products/{productId}": { "schema": "Product", "description": "Branch-scoped product list." },
    "/businesses/{businessId}/branches/{branchId}/transactions/{transactionId}": { "schema": "Transaction", "description": "Branch-scoped sales records." },
    "/businesses/{businessId}/branches/{branchId}/staff/{staffId}": { "schema": "User", "description": "Branch-scoped staff references." },
    "/businesses/{businessId}/branches/{branchId}/customers/{customerId}": { "schema": "object", "description": "Branch-scoped customers." },
    "/businesses/{businessId}/branches/{branchId}/expenses/{expenseId}": { "schema": "object", "description": "Branch-scoped expenses." },
    "/businesses/{businessId}/branches/{branchId}/auditLog/{logId}": { "schema": "object", "description": "Branch-scoped audit logs." }
  }
}
